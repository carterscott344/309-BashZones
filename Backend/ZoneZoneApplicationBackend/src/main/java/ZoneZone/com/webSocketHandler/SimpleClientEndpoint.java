package ZoneZone.com.webSocketHandler;

import javax.websocket.ClientEndpoint;

@ClientEndpoint
public class SimpleClientEndpoint {
    // No logic needed — it's only used to connect + send one-time message
}
